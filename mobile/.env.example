# The API base URL the device can reach. On a physical device use your
# computer's LAN IP (not localhost), e.g. http://192.168.1.5:7060
# The backend (Spring Boot) listens on port 7060.
EXPO_PUBLIC_API_URL=http://localhost:7060
